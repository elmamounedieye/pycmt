name: pycmt_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - jupyter
  - ipykernel
  - xcast
  - cdo
  - cartopy
  - cfgrib
  - pyogrio
  - rasterio
  - rioxarray
  - netcdf4
  - pip
  - pip:
    # Points directly back to your repo to pull the pyproject.toml info
    - git+https://github.com/elmamounedieye/pycmt.git@main